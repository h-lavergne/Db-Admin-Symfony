{% extends 'base.html.twig' %}

{% block title %}Administration creating!{% endblock %}


{% block h1 %} Administration Creating: {% endblock %}

{% block body %}
    {{ form_start(form) }}
    <div class="row">
        <div class="col-6">
            <div>{{ form_row(form.imageFile) }}</div>
            <div>{{ form_row(form.name, {"attr": {"class": ""}, "label": "Food's name"}) }}</div>
            <div>{{ form_row(form.type, {"attr": {"class": ""}, "label": "Food's type"}) }}</div>
            <input type="submit" class="btn btn-primary rounded-pill" value="Create">
        </div>
        <div class="col">
            {{ form_widget(form) }}

        </div>

    </div>
    {{ form_end(form) }}

{% endblock %}
